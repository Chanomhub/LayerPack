# =================================================================
# LayerPack Security Configuration (DEMO)
# =================================================================

# 1. SECURITY KEY (รหัสลับเข้าบ้าน)
# รหัสนี้ใช้สำหรับตรวจสอบสิทธิ์ตอนที่โปรแกรม (React Native / Flutter)
# จะเรียกใช้งาน Native Library (ไฟล์ .node หรือ .so)
# -----------------------------------------------------------------
LPACK_SECURITY_KEY=demo_security_key_2026_change_me


# 2. ENCRYPTION KEY (รหัสลับถอดรหัสไฟล์)
# รหัสนี้ใช้เป็น Master Key สำหรับการถอดรหัส AES-256-GCM 
# ที่อยู่ในไฟล์ .lpack ข้อมูลจะถูกถอดรหัสใน Memory เท่านั้น
# -----------------------------------------------------------------
LPACK_ENCRYPTION_KEY=demo_aes_master_key_999_change_me


# [คำแนะนำในการใช้งาน]
# - ตอน Compile (Build): ต้อง Export ค่าเหล่านี้ก่อนสั่ง build
#   เช่น: export $(cat .env.example | xargs) && cargo build --release
# - ตอน Runtime (ในแอป): นำ LPACK_SECURITY_KEY ไปใส่ใน .env ของแอปคุณ
